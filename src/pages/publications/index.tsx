import React, { useContext } from 'react';
import { Typography } from '@material-ui/core';

const Publications: React.FC = () => {
    return (
        <div>
            <Typography>publications page</Typography>
            <Typography>publications page</Typography>
        </div>
    )
}

export default Publications